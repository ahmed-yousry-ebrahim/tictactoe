<%= form_tag("#{games_path}", method: "post") do %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag :first_player_name, t("games.fp_name") %><br>
    <%= text_field_tag :first_player_name %>
  </div>

    <div class="field">
      <%= label_tag :second_player_name, t("games.sp_name") %><br>
      <%= text_field_tag :second_player_name %>
    </div>
  <div class="actions">
    <%= submit_tag t("games.create"), :class => "button success right" %>
  </div>
<% end %>
